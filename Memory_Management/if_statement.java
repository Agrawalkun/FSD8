package Memory_Management;

public class if_statement {
    int x = 89;                      
        int y = 56;
    public static void main(String[] args) { // Conditional operator(always return true/false in java ) : >,< ,>=,<=,==,!=  
        int x = 89;                          // in C++ conditional operator return 0/1.
        int y = 56;
         if(x>y){ 
            // System.out.println(x<y);  // here x and y is operands and < is operator
// Binary operator contains two operands for the execution and conditional operator comes under the binary operator
// conditional operator also needs logical operator also sometimes
              int y2 =  xcv();
             System.out.println(y2);
             if(y<x){ 
                System.out.println("executed the second if");
             }
        }
  System.out.println("execute the outer if");
    }
       static int xcv()
       {
       

          System.out.println("my name is");
    
           if_statement  x = new if_statement();
           System.out.println(x.x);
           return 0;
        }
        
    }

